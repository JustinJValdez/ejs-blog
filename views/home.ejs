<%- include("partials/header"); -%>
<h1>Home</h1>
<p>
    <%= StartingContent %>
</p>

    <% posts.forEach((post) => { %>
        <h1> <%= post.title %> </h1> 
        <p> <%= post.input.substring(0,100) + "..." %> <a href="/posts/<%=post.title%>">Read More</a> </p> 
    <% }); %>
    
<!--<% for(let i=0; i<posts.length; i++){ %>
    <% console.log(posts[i].Title); %>
<% } %>     
This is a for loop to do the same thing as forEach. forEach is easier to write-->


<%- include("partials/footer"); -%>